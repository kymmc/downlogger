apiVersion: v1
kind: Service
metadata:
  name: downlogger
  namespace: downlogger-prod
  labels:
    app: downlogger
spec:
  type: ClusterIP
  ports:
  - port: 80
    targetPort: 3000
    protocol: TCP
    name: http
  selector:
    app: downlogger

